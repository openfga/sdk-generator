require 'json'
require 'pry'

module JsonHelper
  def load_json(filename)
    file_path = File.join(File.dirname(__FILE__), "../fixtures/#{filename}.json")
    JSON.parse(File.read(file_path))
  end
end
