{{>partial_header}}

from typing import Final

# SDK version information
SDK_VERSION: Final[str] = "{{packageVersion}}"  # Version of the OpenFGA Python SDK

# User agent for HTTP requests
USER_AGENT: Final[str] = "{{userAgent}}"  # User agent used in HTTP requests

# Sample API domain for examples
SAMPLE_BASE_DOMAIN: Final[str] = "{{sampleApiDomain}}"  # Example API domain for documentation/tests

# Retry configuration
RETRY_MAX_ALLOWED_NUMBER: Final[int] = {{retryMaxAllowedNumber}}  # Maximum allowed number of retries for HTTP requests
DEFAULT_MAX_RETRY: Final[int] = {{defaultMaxRetry}}  # Default maximum number of retries for HTTP requests
DEFAULT_MIN_WAIT_IN_MS: Final[int] = {{defaultMinWaitInMs}}  # Default minimum wait time between retries in milliseconds
MAX_BACKOFF_TIME_IN_SEC: Final[int] = {{maxBackoffTimeInSec}}  # Maximum backoff time in seconds
RETRY_HEADER_MAX_ALLOWABLE_DURATION_IN_SEC: Final[int] = {{retryHeaderMaxAllowableDurationInSec}}  # Maximum allowable duration for retry headers in seconds

# Client configuration
CLIENT_MAX_METHOD_PARALLEL_REQUESTS: Final[int] = {{clientMaxMethodParallelRequests}}  # Maximum number of parallel requests for a single method
CLIENT_MAX_BATCH_SIZE: Final[int] = {{clientMaxBatchSize}}  # Maximum batch size for batch requests
DEFAULT_CONNECTION_TIMEOUT_IN_MS: Final[int] = {{defaultConnectionTimeoutInMs}}  # Default connection timeout in milliseconds

# Token management
TOKEN_EXPIRY_THRESHOLD_BUFFER_IN_SEC: Final[int] = {{tokenExpiryThresholdBufferInSec}}  # Buffer time in seconds before token expiry to consider it expired
TOKEN_EXPIRY_JITTER_IN_SEC: Final[int] = {{tokenExpiryJitterInSec}}  # Jitter time in seconds to add randomness to token expiry checks

# HTTP headers
CLIENT_METHOD_HEADER: Final[str] = "{{clientMethodHeader}}"  # Header used to identify the client method
CLIENT_BULK_REQUEST_ID_HEADER: Final[str] = "{{clientBulkRequestIdHeader}}"  # Header used to identify bulk requests
RETRY_AFTER_HEADER_NAME: Final[str] = "{{retryAfterHeaderName}}"  # Standard HTTP header for retry after
RATE_LIMIT_RESET_HEADER_NAME: Final[str] = "{{rateLimitResetHeaderName}}"  # Rate limit reset header name
RATE_LIMIT_RESET_ALT_HEADER_NAME: Final[str] = "{{rateLimitResetAltHeaderName}}"  # Alternative rate limit reset header name
