{{>partial_header}}

from typing import Final

# Version of the OpenFGA Python SDK.
SDK_VERSION: Final[str] = "{{packageVersion}}"

# User agent used in HTTP requests.
USER_AGENT: Final[str] = "{{userAgent}}"

# Example API domain for documentation/tests.
SAMPLE_BASE_DOMAIN: Final[str] = "{{sampleApiDomain}}"

# API URL used for tests.
TEST_API_URL: Final[str] = f"https://api.{SAMPLE_BASE_DOMAIN}"

# API Token Issuer URL used for tests.
TEST_ISSUER_URL: Final[str] = f"https://issuer.{SAMPLE_BASE_DOMAIN}"

# Default API URL.
DEFAULT_API_URL: Final[str] = "{{defaultApiUrl}}"

# Retry configuration

# Maximum allowed number of retries for HTTP requests.
RETRY_MAX_ALLOWED_NUMBER: Final[int] = {{retryMaxAllowedNumber}}

# Default maximum number of retries for HTTP requests.
DEFAULT_MAX_RETRY: Final[int] = {{defaultMaxRetry}}

# Default minimum wait time between retries in milliseconds.
DEFAULT_MIN_WAIT_IN_MS: Final[int] = {{defaultMinWaitInMs}}

# Maximum backoff time in seconds.
MAX_BACKOFF_TIME_IN_SEC: Final[int] = {{maxBackoffTimeInSec}}

# Maximum allowable duration for retry headers in seconds.
RETRY_HEADER_MAX_ALLOWABLE_DURATION_IN_SEC: Final[int] = {{retryHeaderMaxAllowableDurationInSec}}

# Standard HTTP header for retry-after.
RETRY_AFTER_HEADER_NAME: Final[str] = "{{retryAfterHeaderName}}"

# Rate limit reset header name.
RATE_LIMIT_RESET_HEADER_NAME: Final[str] = "{{rateLimitResetHeaderName}}"

# Alternative rate limit reset header name.
RATE_LIMIT_RESET_ALT_HEADER_NAME: Final[str] = "{{rateLimitResetAltHeaderName}}"

# Client methods

# Maximum number of parallel requests for a single method.
CLIENT_MAX_METHOD_PARALLEL_REQUESTS: Final[int] = {{clientMaxMethodParallelRequests}}

# Maximum batch size for batch requests.
CLIENT_MAX_BATCH_SIZE: Final[int] = {{clientMaxBatchSize}}

# Header used to identify the client method.
CLIENT_METHOD_HEADER: Final[str] = "{{clientMethodHeader}}"

# Header used to identify bulk requests.
CLIENT_BULK_REQUEST_ID_HEADER: Final[str] = "{{clientBulkRequestIdHeader}}"

# Connection options

# Default timeout for HTTP requests in milliseconds.
DEFAULT_REQUEST_TIMEOUT_IN_MS: Final[int] = {{defaultRequestTimeoutInMs}}

# Default connection timeout in milliseconds.
DEFAULT_CONNECTION_TIMEOUT_IN_MS: Final[int] = {{defaultConnectionTimeoutInMs}}

# Token management

# Buffer time in seconds before token expiry to consider it expired.
TOKEN_EXPIRY_THRESHOLD_BUFFER_IN_SEC: Final[int] = {{tokenExpiryThresholdBufferInSec}}

# Jitter time in seconds to add randomness to token expiry checks.
TOKEN_EXPIRY_JITTER_IN_SEC: Final[int] = {{tokenExpiryJitterInSec}}

# FGA Response Headers

# Response header name for query duration in milliseconds.
QUERY_DURATION_HEADER_NAME: Final[str] = "{{queryDurationHeaderName}}"
